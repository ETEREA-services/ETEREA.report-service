title: Eterea Core Service
description: Servicio Core para la Gestión de Transferencias, Movimientos Contables y Cotizaciones
theme: jekyll-theme-cayman

# Configuración importante para Jekyll
defaults:
  -
    scope:
      path: "" # aplica a todos los archivos
    values:
      layout: default

# Especificar que los archivos .md deben ser procesados
markdown: kramdown
kramdown:
  input: GFM
  syntax_highlighter: rouge

# Configurar la página principal
index_page: index.md 